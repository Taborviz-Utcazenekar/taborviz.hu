---
interface Props {
  name: string;
  songs: string[];
}

const { name, songs } = Astro.props;
---

<li class="list-row">
  <div class="font-bold w-[clamp(6rem,20vw,12rem)]">{name}</div>
  <div class="text-sm opacity-80">
    {songs.join(" • ")}
  </div>
</li>
